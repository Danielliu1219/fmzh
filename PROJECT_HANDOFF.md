# 项目交接记录

更新时间：2026-09-23（代码上线 GitHub、组员开工材料）

## 1. 项目概况

- **项目名称**：《期末周别慌！》—— 面向大学生期末复习的一站式 AI 智能备考网站
- **项目目标**：学生上传复习资料 + 描述学习状态 → DeepSeek 大模型生成个性化复习重点、按天计划与自测题 → 每日打卡冲刺
- **所属课程**：四川大学计算机学院 2024 级《问题求解实战》（**注意：课程已改名，旧名"研究与开发实践"一律不得再使用**）· 刘添屹小组 · 指导老师陈正茂
- **技术栈**：
  - 前端：Vue 3.5 + Vite 5.4 + Pinia + Vue Router + Element Plus 2.9 + ECharts + Axios
  - 后端：Python / FastAPI + SQLAlchemy 2 + Pydantic v2，SQLite（换 MySQL 只改 `DATABASE_URL`）
  - 外部服务：DeepSeek API（OpenAI 兼容，默认 `LLM_MOCK=true` 不调用）
- **工作目录**：`E:\问题求解实战`（frontend / backend / docs / 交付文档在根目录）
- **Git 状态**：**已建本地仓库并推到 GitHub 公开仓库** `https://github.com/Danielliu1219/fmzh`（main 分支）。本机装了 GitHub CLI 2.101.0，登录账号 Danielliu1219；git 已配置走 gh 令牌（`gh auth setup-git`）。`.gitignore` 已排除 node_modules / .venv / .env / fmzh.db / uploads / dist / __pycache__ / Word `~$` 临时文件 / 需求整理期素材图。

## 2. 本次会话（2026-09-23）完成的工作

**用户需求（原话要点）**：读日志了解之前的工作 → 说明按用户案例现在进行到哪一步 → 把代码上传到 GitHub → 让组员开始各自的工作任务。过程中追加：装 gh 命令行、写开发须知、去掉第 12 页残留的「 ·」、建公开仓库、README 写清楚、PPT 手改确认吸收。

### 2.1 Git 仓库与 GitHub 上线

- 安装 GitHub CLI 2.101.0（winget），用户在浏览器完成设备授权（账号 **Danielliu1219**）；`gh auth setup-git` 让 git 操作走 gh 令牌，推送不再弹登录
- 本地 `git init -b main` → 检查暂存内容 → 分 4 个提交 → 建**公开**仓库 `fmzh` 并推送；线上 HEAD 与本地一致，212 个文件
- 推送前扫描：跟踪文件中无密钥、无手机号/邮箱/学号；线上核对无 `.env`、无 `fmzh.db`、无 node_modules / .venv / uploads
- 仓库话题已加：vue3 / fastapi / deepseek / sqlite / education / course-project

### 2.2 新增 `docs/开发须知.md`（组员开工指南）

六部分：十分钟把项目跑起来（只看界面 / 完整联调两条路）、分支与 PR 流程、**每人负责的用例对应改哪些文件**（精确到 `backend/app/routers/*.py`、`frontend/src/views/*.vue`）、口径红线（课程名 / 公式 / 编号 / 双 mock / 设计语言）、常见坑（加字段要删库重建、`python -X utf8`、npm 镜像等）、求助路径。README §5 目录树与 §10 文档索引已挂入口。

### 2.3 README 按公开仓库标准重写

新增「界面预览」（5 张真实截图，线上直链已验证返回 200）、「项目状态」（需求已交付 + 核心流程可跑通 + 用例进度）、本地运行补 `git clone` 一行；清掉正文里的「痛点」说法（改用"缓解考前焦虑"）。

### 2.4 组长 PPT 手改吸收 + 一处清理

- 用形状级 diff 还原组长在 PowerPoint 里的手改，写回 `build_ppt.py`：第 3 页技术机遇卡删掉「DeepSeek 大模型 API 接入，成本可控、」「内置 Mock 模式保证演示可靠」两行 + 页首导语下移 0.10 英寸；第 4 页删去「DeepSeek」；第 12 页标题去掉「编辑杂志风」；第 15 页分工表刘添屹一栏去掉「全部完成」
- 第 12 页残留的「 ·」经组长确认去掉，重跑 `build_ppt.py` 重新生成 PPTX；与手改备份比对，确认**只有该处文字变化**——另有 4 处文本框高度差异，是 PowerPoint 删字后自动缩框，框透明、文字顶对齐，渲染无差别，故未写进脚本
- 比对脚本留在 `%TEMP%\fmzh-pptdiff\`（`diff.py` 形状级、`dump2.py` run 级、`verify_dotfix.py` 回归验证），下次组长再手改可改路径复用

### 2.5 待办（已告知组长，尚未处理）

- **需求分析 docx 正文仍有 2 处「痛点」**（`docs/需求分析/content.py:44` 与 `:92`），9-17 生成的交付版 docx 里也带着；修改需重新生成 docx，属未授权事项，等组长点头
- **组员要往仓库推分支，需组长把他们的 GitHub 账号加为 collaborator**（Settings → Collaborators，公开仓库只能 clone 不能推）

## 2b. 上次会话（2026-09-19）完成的工作

**用户需求（原话要点）**：项目前期需求分析汇报——做 PPT（项目背景 / 主要功能 / 性能展示），结合需求分析文档；可展示前端内容；图片截不了就留白写备注；另做一份十分钟讲解稿，大纲逻辑清晰；时长十分钟，其余按默认来。

已全部完成：

### 2.1 需求分析汇报 PPT（16 页，16:9）

- 交付物：`需求分析汇报/期末周别慌_需求分析汇报.pptx` + `需求分析汇报/期末周别慌_需求分析汇报_讲解稿.md`（10 分钟逐页台词：一页一句 + 参考台词 + 过渡句 + 时间分配表 + 提问应对）
- 结构：封面 / 目录 / 项目背景（4 难题 + 大模型机遇）/ 项目概述（七步复习闭环）/ 创新点（5 条）/ 功能总览（9 模块 57 用例 + 顶层用例图）/ AI 智能分析（两级权重 + 三档目标）/ 特色功能与公式（压力指数 + 情绪评分）/ 非功能需求（性能 2s/4s/10s/10~60s/50 并发 + 安全 + 可靠）/ 数据描述（6 表）/ 需求分析成果（67/57/72/87 页 V1.2）/ 原型展示 ①登录+首页 ②核心业务页面 / 技术架构 / 进度与分工 / 后续计划与结束
- 视觉沿用前端设计系统：纸 #f0ede3 × 墨 #0c1614 × 珊瑚 #e66348、墨线描边 + 硬阴影；字体华文中宋（标题）/ 微软雅黑（正文）/ Bahnschrift（数字，近似 Space Grotesk）；复用文档图件 fig_3_1_1（顶层用例图）、fig_2_2_1（总体构架图）
- 生成脚本：`需求分析汇报/build_ppt.py`——改内容改脚本重跑（`python -X utf8 build_ppt.py`）；目检流程：PowerPoint COM 转 PDF（SaveAs 32）→ pymupdf 200dpi 渲染 PNG → Read 逐页查看

- **2026-09-20 用户反馈修订**：PPT 初版完成后用户提出"不喜欢太多小字解释，显得很 AI"，已按意见精简——去掉全部注释性小字（文档编号引用、口径说明、脚注式解释、教师角色说明等），页面只留内容本身；S7 重构为"五步链路 + 固定提示词/JSON/重试三个控制标签"、S11 右侧改为"文档已交付"卡片、模块列表去掉灰色小字描述；逻辑关系改用箭头与版式表达，被删内容已并入讲解稿台词。
- **2026-09-20 下午第二轮修订（用户在 PowerPoint 里直接手改过 PPTX）**：
  - 用户手改（已吸收进 build_ppt.py，重跑不丢）：封面删掉"资料给你 · 计划给你 · 高分也给你"灰色标语；目录页右侧数字速览卡调窄（5.38→3.74 宽）、右移、数字与文字贴紧。
  - 本次修订：①全部页面删掉页眉右侧灰色英文小标签（CONTENTS / BACKGROUND 等）与页脚灰色小字（只留右下页码）；②目录页删掉 7 条灰色副标题（含"期末复习的痛点与大模型机遇"），行号改为珊瑚圆形 chip + 标题加大，页面仍保持"数字速览卡"平衡；③**"痛点"一词全部清除**（用户认为不是人话）：S3 红色标签改"难题 01~04"、S5 人文关怀改"缓解考前焦虑"；讲解稿同步改词（真痛点→让大学生头疼、四个痛点→四个难题）；④S6（主要功能）底部 4 个数字条改为"数字+文字同一文本框居中"（修复数字与文字间隔过远）；⑤S13 两处图注、S4"外部系统"标签由浅灰 INK3 加深为 INK2；⑥S16 结束页删掉深色条上灰色落款小字。
  - 备份：`需求分析汇报/期末周别慌_需求分析汇报_用户版备份.pptx`（用户确认新稿后可删）。

### 2.2 前端界面真实截图（6 张，无头 Edge 自动截取）

- 截图在 `需求分析汇报/assets/`：登录 / 首页 / 考试管理 / 复习资料 / AI 分析（×2）。截取方式：起后端（LLM_MOCK=true）+ `seed_demo.py` + 前端 dev server → playwright-core 驱动无头 Edge 登录 demo/demo123456 逐页截图。脚本留在 `%TEMP%\fmzh-shots\shoot.js`（改路径即可复用）
- **已知口径提醒**：首页截图为 USE_MOCK=true 预览态数据（日期锚定 2026-09-07），其余页面为真实后端数据（09-24/09-30/10-03），PPT 内首页已注明"演示数据"。**若要全部真实日期截图，需授权把 USE_MOCK 切回 false 重截**（与未完成事项 #1 联调同一步）
- 本次未改 USE_MOCK、未改任何代码、未动 docx；`seed_demo.py` 已重跑（仅重置 demo 账号演示数据，属预期内）

## 2c. 更早会话（2026-09-17）完成的工作

**用户需求（原话要点）**：情绪公式要改文档；FMZH-USR-0019 缺失需要补齐不要缺失，都提前一位（连续编号）；分工表按照真实分工写；完善需求文档；需求多逻辑多，组长容易忘，需要把逻辑写清楚。

已全部完成，逐项如下：

### 2.1 需求分析 docx 修正（已重新生成并验证）

- **情绪综合评分公式修正**（原公式实际最大约 3.4 且"连续达标因子"未定义）：
  `情绪综合评分 = 0.4×情绪项 + 0.3×完成项 + 0.2×压力项 + 0.1×达标项`，四个分项均归一化到 0~5：
  情绪项 = 5×(自评−1)÷4；完成项 = 5×当日任务完成率；压力项 = 5−压力指数÷20；达标项 = 5×连续达标因子；
  连续达标因子 = min(连续达标天数,7)÷7（连续"当日任务全部完成"的天数）。总分数学上精确落在 0~5。
- **压力指数边界约定补充**：无考试 → 密集度、临近度取 0；考试当天 → 临近度取 1；无任务 → 积压率、完成率取 0。
- **FMZH-USR 编号连续化**：0019 缺口补齐，0020~0068 全部提前一位 → **FMZH-USR-0001~0067 共 67 条连续**；第 8 章引言、表 8-1（72 条对照记录）、数据字典同步更新。
- 修改记录新增 V1.1（修改人刘添屹，2026-09-17）。
- 交付物：`E:\问题求解实战\期末周别慌_系统需求分析.docx`（9 章、78 表、68 图、72 条对照记录）。
- **版式压缩（V1.2）**：A4 页面 + 2.5cm 边距；插图宽度统一缩减（模块图 12cm、活动图 10.5cm）；正文行距 1.5→1.25、段落与标题间距收紧；三张大数据表字号 9→8.5pt；修改记录与目录同页起排；标题/图题设 keep_with_next 防孤行。**全文 119 页 → 87 页，功能与图件数量不变**。根目录另存一份 PDF 预览版供目检。
- 回归流程：改 `docs/需求分析/content.py` → `python -X utf8 build_doc.py`（本地渲染 figures，无需网络）→ `python -X utf8 update_toc.py`（Word COM 更新目录/样式）。

### 2.2 简要用户需求列表重建

- `简要用户需求列表_刘添屹小组.doc`（交付格式）与 `.docx`（源格式）均已重建为 **FMZH-USR-0001~0067 连续编号**，并修复个别笔误（双句号等）。
- 生成脚本：`docs/需求分析/build_user_needs.py`（改需求列表就改这个脚本重跑，自动转 .doc）。

### 2.3 小组分工表填写

- `项目介绍与小组分工.docx` "六、小组分工"已填入真实分工（引言段 + 5 行表格）：
  - **刘添屹（组长）**：现有成果全部由组长完成（需求文档、架构与代码框架、前端页面、统筹）；后续架构把关、代码审查、全文统稿
  - **许丽媛**：账号管理完善 + 考试与日程扩展
  - **胡歆桐**：资料管理扩展 + AI 分析扩展
  - **张诗琪**：学习统计 + 提醒通知 + 番茄钟 + 自测题判分
  - **宋跃月**：特色功能 + 管理端 + 二次确认/日志 + 联调测试
- 与 README 第 8 节、需求分析封面分工说明三处口径一致。

### 2.4 文档同步与新增

- `README.md`：0.5/3.2 两行状态改 🔧 并标注实际差距（后端现有六类资料类型，教材章节/实验报告待接入；PPT/图片解析为扩展方向）；3.3 补压力指数分项定义+分档+边界约定；3.4 替换为新情绪公式；§10 改为 72 条需求对照记录；目录树加入答辩速查。
- `docs/系统设计.md`：需求编号范围改为 0001 ~ 0067。
- **新增 `docs/答辩速查.md`**：演示脚本（7 步）、公式速查、问答要点（10 组预判问题+标准答案）、数字口径表、双 Mock 开关、故障预案、本周行动清单——组长上台前过一遍即可。

## 3. 当前未完成事项（按优先级）

### 必须完成（答辩前）

1. **USE_MOCK 改回 false 并前后端联调验证**（若走真实数据演示）
   - 涉及：`frontend/src/mock/dashboard.js`（当前 **true**，预览态）、`backend/`
   - 步骤：改开关 → 起后端（run.py）→ 跑 `seed_demo.py`（demo/demo123456）→ 注册/登录走全流程
   - 完成标准：首页/课程管理/资料/AI 分析全部读真实接口，无报错
2. **公网部署**（项目介绍文档"八、优先完成"最后一项）：答辩前至少保证内网可演示
3. **用户目检 docx**：抽查 UML 图中文、封面课程名/分工、修改记录 V1.1

### 建议完成

4. **组员开工**：代码已上线 GitHub 公开仓库（组员可直接 clone）→ 组长在 Settings → Collaborators 加组员账号 → 按 `docs/开发须知.md` 认领任务 → 各自 `dev-姓名` 分支 → PR 合并
5. **其余页面精修**（Exams/Materials/Analysis 按设计语言逐页调，当前仅继承全局设计系统）
6. **补齐已知口径差距**：资料类型补教材章节、实验报告（胡歆桐）；任务编辑接口（许丽媛）

### 可选优化

7. 首页 chunk 拆分（echarts 按需引入），消除构建体积警告
8. docx 评审/批准字段填写（评审后）

## 4. 已知问题和风险

1. **USE_MOCK 当前为 true**：登录页出现"免登录预览"按钮、首页读假数据。联调忘记改回 false 会导致"数据对不上"的错觉 —— 最可能的坑
2. **mock 假数据日期固定在 2026-09-07**：USE_MOCK=true 预览时日期不会更新，属正常现象，切回 false 后读真实数据
3. **DeepSeek Key 为空**：`LLM_MOCK=true` 下全流程可用；接真实 API 需去 platform.deepseek.com 申请
4. **前后端已知未对齐处**（README 状态表已标注）：个人资料编辑后端有接口前端无页面；任务编辑后端无接口；提醒通知/特色功能/管理端三模块后端无路由（即组员待开发项）；资料类型后端 6 类 vs 需求文档 7 类
5. **构建体积警告**：echarts + element-plus 全量引入导致 chunk >500KB，仅为提示，不影响功能
6. **中文路径**：项目路径含中文（E:\问题求解实战），部分工具可能异常；目前 npm/vite/python 均正常

## 5. 运行与验证方法

### 取代码

```bash
git clone https://github.com/Danielliu1219/fmzh.git
cd fmzh
```

### 前端（只看界面，无需后端）
```bash
cd "E:/问题求解实战/frontend"
npm install          # 首次
npm run dev          # http://localhost:5173 → 登录页点"免登录预览"
npm run build        # 构建验证
```

### 后端（完整联调）
```bash
cd "E:/问题求解实战/backend"
python -m venv .venv
.venv/Scripts/pip install -r requirements.txt
copy .env.example .env          # 变量名：DATABASE_URL/SECRET_KEY/ACCESS_TOKEN_EXPIRE_MINUTES/UPLOAD_DIR/MAX_UPLOAD_SIZE_MB/LLM_MOCK/DEEPSEEK_API_KEY/DEEPSEEK_BASE_URL/DEEPSEEK_MODEL
.venv/Scripts/python run.py     # http://127.0.0.1:8000，接口文档 /docs
.venv/Scripts/python seed_demo.py    # 重置演示数据（demo/demo123456）
.venv/Scripts/python smoke_test.py   # 冒烟测试
```

### 文档再生成（需求分析 docx）
```bash
cd "E:/问题求解实战/docs/需求分析"
python -X utf8 build_doc.py     # 生成 docx（本地 figures，无需网络）
python -X utf8 update_toc.py    # Word COM 更新目录/样式（需 Word）
python -X utf8 build_user_needs.py   # 重新生成简要用户需求列表 .doc/.docx
```

## 6. 下次会话恢复步骤

1. **先读**：`README.md`（项目全貌）→ 本交接文档 → `docs/答辩速查.md`（答辩口径）
2. **检查**：`frontend/src/mock/dashboard.js` 的 `USE_MOCK` 当前状态（应为 true，预览态；联调时改 false）；端口 5173/8000 是否有服务
3. **继续**：自然入口是 —— (a) USE_MOCK 联调 + 公网部署；(b) 用户目检 docx 后的修改意见；(c) 组员认领任务后的开发支持；(d) 汇报 PPT 修改意见（改 `需求分析汇报/build_ppt.py` 重跑）；重截真实数据截图（先获授权切 USE_MOCK，脚本在 `%TEMP%\fmzh-shots\shoot.js`）
4. **验证**：前端改动后 `npm run build`；后端改动后跑 `smoke_test.py`；文档改动后按第 5 节回归流程
5. **必须先问用户**：USE_MOCK 开关切换、docx 全量再生成（慢但已本地化）、涉及分工/课程名等课程敏感措辞的修改、仓库可见性变更。日常提交与推送到 main 已获授权（2026-09-23 起）

## 7. 重要上下文（跨会话有效）

- **代码仓库**：https://github.com/Danielliu1219/fmzh （公开，main 分支；组员开 `dev-姓名` 分支提 PR，规矩见 `docs/开发须知.md`）
- **用户偏好**：①用自由文本提问，不用 AskUserQuestion 工具；②"这只是作业，不是商业交付产品"——不过度工程化；③UI 已获用户明确认可（"我很喜欢这版"），后续页面沿用同一设计语言；④字体/技术栈细节授权我代决策；⑤用户是组长，需求多逻辑多容易忘，所有公式/口径/分工要写进文档备查
- **硬性口径**：课程名 =《问题求解实战》，旧名"研究与开发实践"**禁止使用**；2024 级；指导老师陈正茂
- **分工口径**：现有成果全部由组长刘添屹完成；许丽媛=账号+考试日程、胡歆桐=资料+AI 分析、张诗琪=统计+提醒+番茄钟、宋跃月=特色功能+管理端+联调测试（改动分工须同步 README 第 8 节 + content.py META + docx 封面 + 项目介绍与小组分工.docx 四处）
- **定稿公式（实现时照抄，口径见 docs/答辩速查.md 第 2 节）**：压力指数 = 30×密集度 + 30×积压率 + 20×(1−完成率) + 20×临近度（含边界约定）；情绪综合评分 = 0.4×情绪项 + 0.3×完成项 + 0.2×压力项 + 0.1×达标项（分项归一化 0~5）；资料权重层级：老师重点＞历年题/练习题＞作业/笔记＞教材/PPT/实验报告；备考目标三档：保及格/稳中等/冲高分
- **编号口径**：用户需求 FMZH-USR-0001~0067（67 条连续）；用例 FMZH-SRS-0.1.0~8.3.0（57 个）；对照表 72 条（67+5 隐藏需求）；文档编号 FMZH-JS-0001
- **设计系统**：--bg-page #f0ede3 / --ink-1 #0c1614 / --coral #e66348 / --coral-bright #f0846b（深底用）；硬阴影 4px 4px 0；数字 Space Grotesk（本地 @fontsource 打包，禁 Google CDN）；旧色 #ff6b4a/#f0ece4 已废弃
- **已否决方案**：PlantUML 标准 base64url 编码（乱码根因）、标题字体走样式表继承（WPS 失效）、Google Fonts CDN（国内不可用）、AskUserQuestion 提问方式（用户不喜欢）
- **曾踩坑**：登录无 mock 兜底（无后端时连登录都进不去，故加了 USE_MOCK 条件渲染的免登录预览按钮）；"不需要后端"的说法曾误导用户，现 README 6.1/6.2/6.3 已把两种运行路径和两个开关写清楚；Windows 控制台 GBK 中文乱码 → 统一 `python -X utf8` 前缀
