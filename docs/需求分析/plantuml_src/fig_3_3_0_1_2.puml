@startuml
skinparam defaultFontName "Noto Sans CJK SC"
start
:用户请求受保护接口;
if (携带有效 JWT?) then (是)
:校验签名与过期时间;
if (会话在有效期内?) then (是)
:放行请求并返回数据;
else (否)
:返回 401，跳转登录页;
endif
else (否)
:返回 401，跳转登录页;
endif
stop
@enduml