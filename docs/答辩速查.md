# 📋 答辩速查（组长上台前 10 分钟过一遍）

> 用途：期末答辩/展示时的演示脚本、问答要点、公式口径一页通。**所有数字口径以本文档和需求分析 docx 为准。**

---

## 0. 上台前检查清单（30 分钟）

- [ ] 后端已启动：`backend\.venv\Scripts\python run.py` → http://127.0.0.1:8000
- [ ] 演示数据已灌入：`backend\.venv\Scripts\python seed_demo.py`（demo / demo123456）
- [ ] 前端已启动：`frontend\npm run dev` → http://localhost:5173
- [ ] **若演示真实数据**：`frontend/src/mock/dashboard.js` 的 `USE_MOCK` 已改回 `false`
- [ ] **若网络不稳/没有 DeepSeek Key**：`backend/.env` 保持 `LLM_MOCK=true`（默认，推荐上台用 Mock）
- [ ] 浏览器开无痕窗口，账号 demo / demo123456 先登录一遍确认全流程
- [ ] 备好一句开场白：项目名 → 一句话是干什么的 → 演示

---

## 1. 演示脚本（7 步主线，约 5 分钟）

> 每一步说什么、看什么，都写好了。照着走即可，语速放慢。

| 步 | 操作 | 台词要点 | 屏幕看点 |
|---|---|---|---|
| 1 | 登录页用 demo / demo123456 登录 | “这是我们的期末备考网站。账号体系带 JWT 登录和密码加密。” | 编辑杂志风登录页 |
| 2 | 课程管理录入一门考试（如 5 天后的高等数学） | “学生先把考试录进来，首页自动倒计时。” | 考试列表 |
| 3 | 复习资料上传一份 TXT/PDF/Word，选科目+来源类型 | “资料要标来源——历年题、老师重点、作业，来源决定它可不可信。” | 上传表单 |
| 4 | AI 复习分析，选科目，输入一句话画像：*“我这门课基本没学，只想及格，极限和积分不太会，还有五天考试，每天能学三小时”* | “学生用一句话描述自己，系统把它转成结构化画像。” | 画像 JSON 展示 |
| 5 | （彩蛋）故意不说天数/时长 → 触发补全表单 | “画像缺字段系统不会瞎猜，会弹表单让用户补全。” | 补全弹窗 |
| 6 | 生成复习分析 | “大模型输出知识点权重、复习提纲、按天计划、自测题——注意每个结论都带依据来源。” | 权重列表+计划 |
| 7 | 回首页看任务自动落库 → 打卡 → 进度更新 | “计划自动变成今天的任务，打卡后首页进度实时联动，形成闭环。” | 首页聚合面板 |

**备用话术**（时间紧可砍步骤 5，合并到 4 里说）。

---

## 2. 公式速查（被问到就背这些）

### 2.1 期末压力指数（0~100）

```
压力指数 = 30×考试密集度 + 30×任务积压率 + 20×(1−任务完成率) + 20×考试临近度
```

- 考试密集度 = min(1, 未来 7 天考试场次÷4)
- 任务积压率 = 逾期未完成任务数÷总任务数
- 任务完成率 = 已完成任务数÷总任务数
- 考试临近度 = min(1, 21÷最近一场考试剩余天数)
- 分档：**<40 轻松 / 40~60 适中 / 60~80 偏压 / ≥80 高压**（≥70 建议开启救急模式）
- **边界约定**：无考试 → 密集度、临近度取 0；考试当天（剩余 0 天）→ 临近度取 1；无任务 → 积压率、完成率取 0

### 2.2 情绪综合评分（0~5）

```
情绪综合评分 = 0.4×情绪项 + 0.3×完成项 + 0.2×压力项 + 0.1×达标项
```

| 分项 | 公式 | 说明 |
|---|---|---|
| 情绪项 | 5×(情绪自评−1)÷4 | 自评 1~5 |
| 完成项 | 5×当日任务完成率 | |
| 压力项 | 5−压力指数÷20 | |
| 达标项 | 5×连续达标因子 | 连续达标因子 = min(连续达标天数,7)÷7，连续达标天数 = 连续「当日任务全部完成」的天数 |

> 答辩点：**四个分项都归一化到 0~5 再加权，总分数学上精确落在 0~5**，不会出现“满分 5 分实际最高 3.4”的口径漏洞。

### 2.3 两级权重 + 三档目标

- **资料权重**（资料可不可信）：`老师标注重点 ＞ 历年试题/练习题 ＞ 平时作业/课堂笔记 ＞ 教材章节/PPT/实验报告`
- **知识点权重**（知识点重不重要）：历年题必考点 → 基础值，再按用户画像（目标/薄弱章节）调整
- **备考目标三档**：保及格 / 稳中等 / 冲高分 → 不同档推荐不同内容（薄弱者先高频基础题，冲高分者推综合题难题）

---

## 3. 问答要点（老师/同学预判）

**Q：为什么用大模型，不用写死规则？**
A：系统的输入是「多来源、非结构化的复习资料 + 学生自然语言描述」，规则写死无法覆盖。我们不是放任大模型自由发挥——三层控制：① 自然语言先转**固定 JSON 画像**；② **固定提示词**约束角色与范围；③ 输出**固定 JSON Schema**，解析失败有兜底重试，且默认 Mock 模式保证演示永远可用。

**Q：大模型会胡说八道，复习建议错了谁负责？**
A：第一，回答范围限定在“基于用户上传资料 + 课程资料 + 考试目标”（USR-0048）；第二，页面明示“AI 生成内容仅供复习参考”（USR-0049）；第三，每个结论带**推荐依据**（来自历年高频题/老师重点/用户薄弱项），用户可自行判断。

**Q：压力指数、权重这些公式哪来的？**
A：团队自定义的**可解释加权模型**：来源层级给资料权重、历年题频次定知识点基础权重、画像做个性化调整；压力指数四个分项各有明确定义和边界约定（无考试/无任务/考试当天都有规定），不是拍脑袋。

**Q：跟超级课程表、滴答清单这类 App 有什么不同？**
A：它们做通用日程；我们**只做期末冲刺这一个场景**：多源资料整合分析（不是单文件总结）、按目标和基础差异化推荐（不是所有人同一套计划）、压力指数/救急模式/今日三件事这些人性化功能直面期末焦虑。

**Q：为什么用 SQLite 不直接用 MySQL？**
A：需求文档口径：开发与演示阶段以 SQLite 实现等价替代，切换只需改 `DATABASE_URL` 连接配置一行，SQLAlchemy 模型不用动。

**Q：安全性怎么保证？**
A：JWT 会话（7 天）+ bcrypt 密码哈希；后端所有查询从令牌解析用户强制按用户过滤（数据隔离）；连续 3 次密码错误锁定账号；重要操作二次确认与操作日志在开发计划中（README 状态表）。

**Q：哪些功能还没做完？**
A：诚实口径——MVP 主线（注册登录→录考试→传资料→AI 分析→任务打卡→统计）已全流程打通；**未完成项都写进了 README 状态表并分到了人**：提醒通知、特色功能、管理端三个模块待开发，资料类型后端现有六类、教材章节/实验报告待接入，任务编辑、考表导入等扩展项排期中。演示时按“已完成 + 已规划”如实讲，不隐瞒。

**Q：隐藏需求是什么？**
A：原始需求列表没提、但系统必要的内容，共 5 条（如数据隔离、操作日志），需求分析 2.4 节已补充并纳入对照表。

**Q：这个项目后续怎么做？**
A：本周按 README 分工表推进组员开发 + 前后端联调 + 公网部署（项目介绍文档“八、优先完成”），Git 分支协作 + PR 合并。

---

## 4. 数字口径速查（说错数字最减分，这里统一）

| 口径 | 数字 | 出处 |
|---|---|---|
| 用户需求编号 | **FMZH-USR-0001 ~ 0067（67 条连续）** | 简要用户需求列表.doc |
| 功能模块 | **9 个**（公共 + 账号/考试日程/资料/AI 分析/统计/提醒/特色/管理端） | 需求分析 3.1 |
| 用例数 | **57 个**（FMZH-SRS-0.1.0 ~ 8.3.0） | 需求分析 3.2 |
| 需求对照记录 | **72 条**（67 用户需求 + 5 隐藏需求） | 需求分析表 8-1 |
| 需求分析文档 | 9 章、**78 张表、68 张 UML 图**、全文 **87 页（A4）**、V1.2（2026-09-17） | 期末周别慌_系统需求分析.docx |
| 数据库表 | **6 张**：users / exams / materials / user_profiles / analyses / tasks | backend/app/models |
| 演示账号 | demo / demo123456 | seed_demo.py |
| 课程口径 | 《**问题求解实战**》2024 级，指导老师陈正茂（**旧名“研究与开发实践”禁止出现**） | — |

---

## 5. 两个 Mock 开关（现场出故障时救命）

| 开关 | 位置 | 当前 | 作用 |
|---|---|---|---|
| `USE_MOCK` | `frontend/src/mock/dashboard.js` | **true**（预览态） | true = 首页读假数据、登录页出现“免登录预览”按钮；联调真实后端必须改回 false |
| `LLM_MOCK` | `backend/.env` | **true** | true = 大模型返回内置示例，**不需要网络和 Key**，全流程可演示 |

**故障预案**：AI 分析报错 → 确认 `LLM_MOCK=true`；首页数据对不上 → 检查 `USE_MOCK=false` 且后端已启动；DeepSeek 没 Key → 保持 Mock 即可，答辩口径“演示阶段用 Mock，接入真实 API 只改配置”。

---

## 6. 本周行动清单（按优先级）

1. **前后端联调**：`USE_MOCK` 改回 false，跑通注册→录入→上传→AI 分析→打卡全流程（依赖：后端 + seed_demo）
2. **组员开工**：分发代码 → 按 README 第 8 节分工表认领 → 各自分支开发 → PR 合并
3. **补齐已承诺口径**：资料类型补教材章节、实验报告两类（胡歆桐）；任务编辑接口（许丽媛）
4. **公网部署**：项目介绍文档“八、优先完成”的最后一项，答辩前至少跑通内网演示
5. **文档回归**：任何公式/口径/接口改动，同步改 `docs/需求分析/content.py` → build_doc.py → update_toc.py（重生成 docx），并更新本文件

---

## 7. 演示环境速启（一条条敲）

```bash
# 终端 1：后端
cd E:\问题求解实战\backend
.venv\Scripts\python run.py
# 终端 2：灌演示数据
.venv\Scripts\python seed_demo.py
# 终端 3：前端
cd E:\问题求解实战\frontend
npm run dev
```

浏览器开 http://localhost:5173 → demo / demo123456。
